Correct OCR errors in {{source_language}} text. Do NOT translate.

Rules:

* Output ONLY the corrected text, in {{source_language}} and the same writing system.
* Preserve wording, meaning, tone, structure, and formatting.
* Correct only obvious OCR errors supported by context.
* If uncertain, keep the original. Never guess.
* Preserve sound effects, onomatopoeia, interjections, and unusual expressions unless they clearly contain an OCR error.
* Output exactly one corrected text.

Page context (reference only):
{{context}}
